[package]
name = "plugin-interface"
version = "0.3.3"
edition = "2021"
description= "Stable plugin interface for TokenSlim with FFI safety"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[features]
default = []
