//! 代码脱水：保留内置异常和关键字标识符，折叠长自定义标识符为字典令牌，压缩连续空格为长度标记。

//! ## 保留信号
//! - SyntaxError（JavaScript / Python 内置异常）
//! - public（关键字）
//! - short_id（长度≤8的标识符）

//! ## 压缩目标
//! - 长自定义标识符（长度>8且非关键字非保留异常）压缩为$PK令牌
//! - 连续空格压缩为$S|长度标记
//!
//! ## 转义约定（P2-78）
//! 源码中的字面 `$` 在压缩时统一转义为 `$$`；解压端先判定 `$$`（还原为字面 `$`），
//! 再判定单 `$` 引导的 `$S|N`/`$PKn` 标记——原文中的 `$S|5`、`$PK1` 字面量不会被误还原，
//! 往返（round-trip）保持字面一致。
pub mod methods;
//
pub mod types;

pub use types::*;

#[cfg(test)]
mod test;

#[cfg(test)]
mod showcase;
