# 插件名称：NoiseFilterPlugin

## 1. 模块概述
作为管线的“最后一道防线”，识别并屏蔽无法被其它插件理解的长字符串噪声（如 Base64 片段、Memory Dump、超长随机 Hash 等）。

## 2. 功能点清单
### 2.1 启发式低熵文本检测
- **功能描述**: 使用算法检测字符分布，识别非自然语言的长字符串。

### 2.2 无意义哈希截断
- **功能描述**: 将 128 位及以上的无意义随机字符流替换为简短的哈希标记（如 `[Hash:abc1...]`）。
