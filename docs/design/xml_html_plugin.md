# XML/HTML Plugin 模块设计

## 职责
专门用于处理 XML 或 HTML 结构文本的压缩。去除大量为了人类阅读设计的换行、空格缩进，并通过字典折叠频繁出现的长属性名称（例如 `class`, `style`, `data-v-xxxxx`）及标签对。

## 设计
- 使用 `quick-xml` 库的流式读取器处理，遇到长属性值和闭合标签进行压缩。
- 在 MVP 阶段，主要任务是剔除节点间的无意义文本（全空格），提取长属性和类名到宏字典。
