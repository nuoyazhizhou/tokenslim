# 代码注释日志 Q 问题修订任务清单

> 来源：`docs/reports/代码注释工作日志.md`（代码注释专家自动任务，问题「仅记录未改」）。
> 范围：从日志 177 条去重 Q 中筛出的 51 条高危项（正确性/健壮性/协议失效/死代码）。
> 处置原则：**每项先二次确认真伪（读真实源码），确认是问题再造单修正**；日志自称「已修复」的条目须复核。
>
> **状态（2026-09-05 收口）**：本轮已完成二次核对，P0 全部处置；P1/P2 中确凿且低回归风险项已修复——Q58/81/477/496/485/504 亦已实改并回填本表。「不动/记录」项为经源码核实的设计取舍、能力边界或显式标注的功能增强。全量 lib 回归 **1158 passed**。

## 分级

- **P0 正确性/panic/协议失效**：不修会导致 panic、dptr空操作、判定错位等硬伤。

- **P1 逻辑 bug/误判**：影响分类/解析正确性。

- **P2 死代码/记录/可维护性**：未接线冗余、占位实现，清理类。

## 修订清单（处置结果已回填）

### P0 正确性 / panic / 协议失效

| 项     | Q#   | 问题                                                    | 二次确认                                                                | 处置                                         |
| ----- | ---- | ----------------------------------------------------- | ------------------------------------------------------------------- | ------------------------------------------ |
| P0-1  | Q518 | db\_log `&raw[..80]`/`[..120]` 字节截断多字节 → panic        | ✅ 属实（UTF-8 中文字段）                                                    | **已修** `truncate_utf8_safe` 字符边界截断         |
| P0-2  | Q517 | db\_log 压缩/解压协议前缀不一致 → 解压空操作                          | ✅ 属实（decompress 查 `$DB\|` 而压缩无此前缀）                                  | **已修** 重写 `decompress` 识别真实 `PG\|/MY\|` 前缀 |
| P0-3  | Q74  | node\_error `\|` 分隔 splitn 错切                         | ❌ **非真问题**：decompress 已用 `splitn`，尾部字段含 `\|` 会完整保留；func/路径不可能含 `\|` | 不动，记录                                      |
| P0-4  | Q525 | k8s 固定取 message/log 字段，空字段丢正文                         | ✅ 属实（空串候选直接返回）                                                      | **已修** 加非空判断继续尝试下一字段                       |
| P0-5  | Q510 | ci\_log 固定 `[..28]` 切片剥 GH 时间戳，变长精度失效                 | ✅ 属实                                                                | **已修** 按 `Z `     分隔符动态定位 + 短行长度保护         |
| P0-6  | Q532 | vcs\_fossil `abs_end+2` 硬切多字节 → panic                 | ✅ 属实（紧邻中文字符越界）                                                      | **已修** `get`+`strip_prefix` 安全定位           |
| P0-7  | Q84  | pipeline\_bench `partial_cmp().unwrap()` 遇 NaN panic  | ✅ 属实                                                                | **已修** `.unwrap_or(Ordering::Equal)`       |
| P0-8  | Q444 | dictionary resolve\_for\_ai `$FL` 调无此分支 → 恒 None      | ✅ 属实（`$FL` 非合法前缀）                                                   | **已修** 移除误导分支、注明 `$C` 为 flags 前缀           |
| P0-9  | Q448 | doctor\_workspace 重复 `primary=="node"` → NestJS 分支不可达 | ✅ 属实（外层 node 分支先拦截）                                                 | **已修** 并入主 node 块内                         |
| P0-10 | Q449 | doctor\_workspace `contains("20")` 等 → 2011 误判 c++20  | ✅ 属实                                                                | **已修** 精确提取标准数字                            |

### P1 逻辑 bug / 误判

| 项     | Q#   | 问题                                            | 二次确认                       | 处置                                       |
| ----- | ---- | --------------------------------------------- | -------------------------- | ---------------------------------------- |
| P1-1  | Q31  | vcs\_p4 布尔缺括号                                 | ✅ 行为正确（`&&` 优先），仅可读性       | **已修** 补显式分组                             |
| P1-2  | Q47  | git\_diff `(?P<old>.*)` 贪婪，碰上含     ` b/` 路径错截 | ✅ 属实，`.*?` 停在首分隔符更稳        | **已修** 改非贪婪                              |
| P1-3  | Q58  | sql 关键词含 IN/LIKE/ALL/EXISTS，`matches>=2` 过宽误检 | ✅ 风险真实（英文词触检）              | **已修** `SQL_HEAD_RE` 强动词前置门槛，detect 首选动词 |
| P1-4  | Q63  | dotnet 裸 `System.` +0.3                       | ❌ 单现 0.3 不 `>0.3`，不触发      | 不动，记录                                    |
| P1-5  | Q67  | spring\_boot 裸 `Spring Boot` +0.5 触发          | ✅ 属实（单现即 0.5>0.3 误路由）      | **已修** 需叠生命周期/下载信号方加分                    |
| P1-6  | Q59  | sql `VALUES` `.*` 贪婪（信息性）                     | ✅ 贪婪捕获整段值实为预期              | 不动，信息性                                   |
| P1-7  | Q77  | nodejs `optimize_typescript` 捕获过宽             | ✅ 属实，但整函数死代码               | **已修**（随 Q78 整体删除死函数）                    |
| P1-8  | Q81  | markdown normalize `.*?` 跨行                   | ✅ 属实（无 query 链接遇行内 `?` 误吞） | **已修** `link_re` 限定 URL 内、遇 `)`/空白即止     |
| P1-9  | Q88  | log\_reorder PATH\_RE 任意盘符 vs 仅缩 C:/D:        | ✅ 口径不一致                    | **已修** 守卫改为任一 `X:`                       |
| P1-10 | Q462 | extract\_json\_object 以首个 `{` 起，前导串内括号误当起点    | ✅ 属实（前导字符串内 `{`）           | **已修** 串首扫描跳过串内 `{`                      |
| P1-11 | Q477 | strip\_env\_prefix 空格拆 KEY=value，引号/空格值拆错     | ✅ 属实（`FOO="a b"` 拆错）       | **已修** `tokenize_quote_aware` 引号感知切分     |
| P1-12 | Q479 | rule\_diagnosis 字符串相等判冲突，正则语义重叠漏检             | ✅ 属实（诊断工具能力边界）             | 不动，记录（语义重叠检测需正则状态机求交，代价高且易误报）            |
| P1-13 | Q492 | text\_slicer slice\_paragraph 跨分片段落切断         | ✅ 属实但为流式/并行契约              | 不动，记录（段落跨分片切断系有界内存冲刷的固有设计）               |
| P1-14 | Q496 | restructure\_as\_tree 正则编译失败静默回退              | ✅ 属实（无误报提示）                | **已修** 失败回退时记录 `tracing::warn`           |

### P2 死代码 / 记录 / 可维护性

| 项     | Q#   | 问题                                                      | 二次确认                                | 处置                               |
| ----- | ---- | ------------------------------------------------------- | ----------------------------------- | -------------------------------- |
| P2-1  | Q40  | text\_compact `MAX_DIFF_LINES` allow(dead\_code) 无引用    | ✅ 仅定义、无使用                           | **已修** 移除                        |
| P2-2  | Q53  | k8s `K8S_PREFIX_RE` allow(dead\_code) 未用                | ✅ 仅定义、无使用                           | **已修** 移除                        |
| P2-3  | Q56  | sql `SENSITIVE_COLS_RE` allow(dead\_code) 未用            | ✅ 仅定义、无使用                           | **已修** 移除                        |
| P2-4  | Q445 | dictionary path\_hierarchy/语义别名未接线                      | 属既有未启用子系统                           | **延后**：接入涉及范围大，非本轮               |
| P2-5  | Q485 | core 阈值 `StorageType`/`SystemInfo` from\_file 元数据恒 None | ✅ 属实（死代码）                           | **已修** 删除 StorageType/SystemInfo |
| P2-6  | Q491 | `Box::leak` 泄漏配置名                                       | ✅ 系刻意取 `'static` 生命周期，多处同模式         | 不动，记录                            |
| P2-7  | Q37  | gcc `replace_macros_in_text` `-DMACRO=` 占位透传            | ✅ 已显式标注「空操作原样返回」（intentional no-op） | 不动，记录（压缩宏属功能增强）                  |
| P2-8  | Q78  | nodejs 旧 `optimize_*` 5 函数未接线（两套口径）                     | ✅ 全仓库无任何调用者                         | **已修** 整体删除死函数                   |
| P2-9  | Q537 | vcs\_cvs `let _ = status;` 冗余绑定                         | ✅ 冗余                                | **已修** 改用 `(_, path)` 解构         |
| P2-10 | Q504 | artifact\_summary SARIF 前 12 截断、RULES 取 6 非按频次          | ✅ 属实（RULES 字母序非频次）                  | **已修** RULES 按频次降序取 top6         |
| P2-11 | Q515 | cloudformation `lines.contains(&failure)` O(n)          | ✅ 低效但量级小                            | 不动，记录                            |

### 2026-09-10 追加：骨架遗产处置（Q413 / Q414 / Q415 / Q428）

> 来源：`docs/reports/Codereview 与代码注释问题闭环核验（TokenSlim · 2026-09-10）.md` §3.2 遗留项。
> 方案：`docs/plans/Q413-Q428 遗留骨架遗产处置方案（TokenSlim · 2026-09-10）.md`。
> 处置方式：**删除**（判据：零消费 / 目标 API 已不存在 / 已有真实测试覆盖）。

| 项    | Q#   | 内容                                                              | 二次确认                                            | 处置                                          |
| ---- | ---- | --------------------------------------------------------------- | ----------------------------------------------- | ------------------------------------------- |
| P2-12 | Q413 | `DedupConfig.fuzzy_threshold` + `DedupEngine`/`SharedDedupEngine.fuzzy_cache` 零消费死字段 | ✅ 全仓无第三处引用；`config/` 无 fuzzy 键；结构无 `deny_unknown_fields` | **已修** 删 2 字段 + TODO + `allow(dead_code)` + 12 处构造/字面量 |
| P2-13 | Q414 | `tests/test_compression.rs` 未完成骨架（0 `#[test]`）                   | ✅ 属实；按 `.log` 过滤而 `tests/data` 无 `.log`          | **已修** 删除文件                                 |
| P2-14 | Q415 | `tests/test_vocabulary.rs` 手写模拟冒充集成测试（0 `#[test]`）               | ✅ 属实；**全仓已无 `Vocabulary` 符号**，接入无处可接              | **已修** 删除文件                                 |
| P2-15 | Q428 | `tests/test_integration.rs` `str::replace` 模拟字典（0 `#[test]`）      | ✅ 属实；真实往返已由 `e2e_roundtrip_regression` 覆盖         | **已修** 删除文件                                 |

**勘误**：核验报告 §3.2 称三骨架文件「不参与 `cargo test`」。实测 `cargo metadata` 三者均为正式 `test` target（本仓无 `[[test]]`、无 `autotests=false`），且 `rustc --test` 允许自定义 `fn main` → 实为「**参与编译、0 测试**」，非「不参与」。

### 需复核「已修复」标签

| 项      | Q#  | 核实结果                              | 处置                                                                   | <br />               |
| ------ | --- | --------------------------------- | -------------------------------------------------------------------- | :------------------- |
| <br /> | R-1 | Q75                               | ✅ 属实：`compress_tsc_output_v2` doc 写成「压缩 npm install 输出」，实为 TS 编译错误聚合 | **已修** doc 改为 tsc 描述 |
| R-2    | Q79 | nodejs 无 `normalize` 方法，「四处」仅三处可核 | 引用失效，无法完整确认                                                          | 记录，不追施加改             |

### 已确认修复（无需改动）

Q45（build\_regex 已接入 log\_miner）、Q442（字典 `$PK` 前置于 `$P`）、Q450（`.m` 后缀区分）。

## 执行 & 收口

- 每项均已读源码二次确认，确认真问题才改，临界项记录延后原因。

- 全量 lib 回归：**1158 passed / 0 failed**（含所有本轮改动）。

- 本轮改动清单见 `docs/reports/classifier_gap_report.md` 备注；未做改动已删除产物，无字节对外。

